<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Signin Page</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: cadetblue;
            background-image: url('https://cdn.glitch.global/3c22ae8b-fb74-4296-a319-f74530317056/pexels-nataliya-vaitkevich-5554751.jpg?v=1697114445043');
            background-position: center;
            background-repeat: no-repeat;
            background-size: cover;
            height: 100vh;
            font-family: 'Arial', sans-serif;
        }

        .container {
            max-width: 500px;
            margin: 0 auto;
            padding-top: 10vh;
            background-color: rgba(255, 255, 255, 0.8);
            border-radius: 10px;
            box-shadow: 0 0 15px rgba(0,0,0,0.2);
        }

        h1 {
            text-align: center;
            margin-bottom: 40px;
            color: #333;
        }

        label {
            font-weight: bold;
            color: #555;
        }

        input[type="text"], 
        input[type="email"], 
        input[type="password"] {
            width: 100%;
            padding: 10px;
            margin-bottom: 10px;
            border-radius: 5px;
            border: 1px solid #aaa;
        }

        input[type="submit"] {
            background-color: #007bff;
            color: #fff;
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        input[type="submit"]:hover {
            background-color: #0056b3;
        }

        form {
            padding: 20px;
        }

        fieldset {
            border: 1px solid #ccc;
            padding: 10px;
            border-radius: 5px;
            transition: transform 0.3s ease;
        }

        fieldset:hover {
            transform: scale(1.05);
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.3);
        }

        legend {
            color: #555;
        }

    </style>
</head>
<body>
    <div class="container">
        <h1>Welcome to the Signin Page</h1>
        <form action="signinsubmit" method="GET">
            <fieldset>
                <legend>Sign In</legend>
                <label for="email">Email ID:</label>
                <input type="text" id="email" name="email"> 
                <label for="password">Password:</label>
                <input type="password" id="password" name="password"> 
                <label for="code">Your Code (student/teacher):</label>
                <input type="text" id="code" name="code"> 
                <input type="submit" value="Sign In">
              <center><p><%=data1%></p></center>
            </fieldset>
        </form>
    </div>
</body>
</html>
